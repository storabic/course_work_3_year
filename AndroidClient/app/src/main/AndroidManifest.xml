<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cw.androidclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_myplaces"
        android:label="PayTogether"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidClient"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.login.PaymentActivity"></activity>
        <activity android:name=".ui.login.SubjectCreateDifferentSumActivity" />
        <activity android:name=".ui.login.SubjectActivity" />
        <activity android:name=".ui.login.SubjectCreateActivity" />
        <activity android:name=".ui.login.EditGroupActivity" />
        <activity android:name=".ui.login.SubjectEditActivity" />
        <activity android:name=".ui.login.GroupMainActivity" />
        <activity android:name=".ui.login.CreateGroupActivity" />
        <activity android:name=".ui.login.MenuActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>